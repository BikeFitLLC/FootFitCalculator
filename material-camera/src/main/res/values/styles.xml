<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="MaterialCamera.CaptureActivity" parent="Theme.AppCompat.NoActionBar">
        <item name="colorAccent">@android:color/white</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <!-- Wedge calculator -->
    <style name="TitleBar">
        <item name="android:background">@color/titlebarBackground</item>
    </style>

    <style name="TitleBar.ThemeOverlay" parent="">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlHighlight">#3fffffff</item>
        <item name="android:background">@color/titlebarBackground</item>
    </style>

    <style name="TitleBar.PopupThemeOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@color/mcam_color_light</item>
    </style>

</resources>